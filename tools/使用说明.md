古琴打谱用工具

```sh
# 安装依赖
pip install -r requirements.txt

# 使用默认 F 大调正调
python3 guqin_tuning_calculator.py

# 自定义调弦（例如 Bb大调 紧五弦定弦）
python3 guqin_tuning_calculator.py --tuning C2 D2 F2 G2 A#2 C3 D3
# 导出到 Markdown 文件
python3 guqin_tuning_calculator.py --output 我的调弦.md --name "F大调正调"
```